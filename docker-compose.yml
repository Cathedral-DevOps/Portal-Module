services:
  web:
    build: .
    container_name: portal_flask_app
    restart: unless-stopped
    ports:
     - "127.0.0.1:8005:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    mem_limit: 512m
    mem_reservation: 256m